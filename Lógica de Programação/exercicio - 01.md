`````js

var minhaVar , soma , fazSol ;
var comida = [ ];

minhaVar = (10);
soma = 15 + 8;
soma++;
soma *= 3;
fazSol = true;
comida = ['arroz', 'feijão', 'ovo'];
console.log(comida[1]);
console.log (soma === minhaVar);
console.log( minhaVar <= soma);

function divisao (soma , minhaVar){
    return soma / minhaVar
}

console.log (divisao(soma , minhaVar));
